## What and why

Briefly, what does this change and why.

## Checklist

- [ ] `uv run pytest`, `uv run ruff check .`, and `uv run mypy` pass
- [ ] New behaviour has tests (rules/heuristics run on diff lines, not whole files)
- [ ] No new network calls in the default path
